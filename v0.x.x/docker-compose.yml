version: "3.8"

services:
  app:
    image: superklok/swingcarsdotnet:0.5.13
    build: 
      context: .
    ports:
      - "80:80"
    command: node app.js
    volumes:
      - .:/app
    environment:
      - NODE_ENV=production